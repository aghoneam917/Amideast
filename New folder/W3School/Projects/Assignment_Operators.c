#include<stdlib.h>
#include<stdio.h>
int main(){
      int x=5,y=3;
      //x+=5; add 5 to x's value
      //x-=5; subtract 5 from x's value
      //x*=5; Multiply 5 to x's value 
      //x/=5; divide x's value over 5
      //?x&=5;
      //?x|=5;
      //x%=5; display the reminder of the division
      //?x^=5;
      //x<<=5; //In these last 2 operation we go to convert the x value into binary sys. then we add 2 zero's form the right and convert it again
      //x>>=5; //here we add 2 zero's from the left
      printf("%d",x);
      return 0;
}